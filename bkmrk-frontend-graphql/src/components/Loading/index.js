import React from 'react';

import Content from '../Content';
import P from '../P';

const Loading = () => {
  return (
    <Content>
      <P>Loading items....</P>
    </Content>
  );
};

export default Loading;
